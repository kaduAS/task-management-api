# Build Summary

A simple task API with user auth and database. Code lives in src/, schema in prisma/, plus docs. Use this project to show employers you can build and deploy a backend.

### Key points
- 10 endpoints (auth + CRUD tasks)
- Uses JWT for security
- Prisma handles database
- Live instructions included

Next: run setup, push to GitHub, deploy, add to resume.
